; go test -v -run CompareOutputTable$ ./ompp/db
; go test -v -run TestCompareOutputTable ./ompp/db
; go test -v -run ^TestCompareOutputTable$ ./ompp/db
;

; go test -v -run CompareOutputTable$ ./ompp/db
;
[CompareOutputTable]
ModelName      = modelOne
ModelDigest    = 
DbPath         = ../../../test/modelOne.sqlite
TableName      = salarySex

Calculate_1    = expr0 - expr1
IsAggr_1       = false
BaseRunId_1    = 201
RunIds_1       = 202,205

; Calculate  = expr0[variant] / OM_DIV_BY(expr0[base])
; Calculate  = expr0[variant] - OM_IF( expr0[base] > 0.1 THEN expr0[base] ELSE 0.1 )
