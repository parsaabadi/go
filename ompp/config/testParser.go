// We want to test the function:
// loadIni(iniContent string) (map[string]string, error)

// We can use the existing NewIni function for convenience:
// NewIni(iniPath, encodingName string) (map[string]string, error)

package config

// Since I'm defining this to be part of the config package
// I don't need to import NewIni since it's in package scope.

func main() {
    // Call NewIni on a test file and print to stdout to 
    // verify your changes.


}
